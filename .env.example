# ============================================
# SERVER CONFIGURATION
# ============================================
# PORT=3000  # Optional - defaults to 3000 if not set

# ============================================
# AUTHENTICATION (REQUIRED)
# ============================================

# Admin UI Credentials - Required for accessing the admin interface
UI_USERNAME=admin
UI_PASSWORD=admin

# Master API Key - Has access to all models, bypasses model restrictions
# This key is stored in .env only, not in database
MASTER_API_KEY=sk-your-master-key-here

